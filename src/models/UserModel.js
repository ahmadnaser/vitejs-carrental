export class User {
  constructor(
    email,
    password,
    name,
    phone,
    address,
    role
  ) {
    this.email = email; 
    this.password = password;
    this.name = name;
    this.phone = phone;
    this.address = address;
    this.role = role; 
  }
}
