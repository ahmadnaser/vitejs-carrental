export class Garage {
  constructor(garage_id, name,type, location, contact_info, garage_info) {
    this.garage_id = garage_id;
    this.name = name;
    this.type = type;
    this.location = location;
    this.contact_info = contact_info;
    this.garage_info = garage_info;
  }
}